# DocAI 智能文档处理系统 — 答辩材料

> 本文档为项目答辩准备材料，涵盖技术架构、功能实现、创新要点、常见问答等内容。

---

## 一、项目概述

### 1.1 项目背景与问题

在日常办公中，工作人员需要从大量格式不一的文档资料中人工阅读、提取关键信息并手动整理到 Excel 表格或数据库。这一过程：
- 重复性高、操作繁琐
- 容易因疲劳导致疏漏和错误
- 严重消耗本应用于核心业务的时间和精力

### 1.2 解决方案

DocAI 是一款基于大语言模型（LLM）的智能文档处理系统，实现：
- **文档智能编辑交互**：通过自然语言指令与 AI 对话，完成文档分析、编辑、润色
- **非结构化文档信息提取**：自动识别并提取文档中的关键字段和实体信息
- **表格自定义数据填写**：从已提取的数据中自动匹配并填充模板表格

### 1.3 核心指标

| 指标 | 目标值 | 实现方案 |
|------|--------|---------|
| 信息提取准确率 | >80% | 大模型结构化抽取 + 字段标准化 |
| 表格填充准确率 | >90% | 规则优先 + LLM辅助 + 多级置信度 |
| 单模板响应时间 | <60秒 | 预提取架构 + 超时控制 + 并行处理 |

---

## 二、技术架构

### 2.1 系统架构图

```
┌──────────────────────────────────────────────────────┐
│                    前端展示层                          │
│  Vue 3 + Element Plus + Pinia + Vite                 │
│  (SPA单页应用, 响应式设计)                              │
└──────────────────┬───────────────────────────────────┘
                   │ HTTP / SSE
┌──────────────────┴───────────────────────────────────┐
│               API网关层 (Spring Cloud Gateway)         │
│  路由转发 / JWT认证 / 负载均衡 / 超时控制                 │
└──────────────────┬───────────────────────────────────┘
                   │
┌──────────────────┴───────────────────────────────────┐
│                  微服务层                               │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐     │
│  │用户服务  │ │文件服务  │ │AI服务   │ │MCP服务  │     │
│  │(9001)   │ │(9003)   │ │(9002)   │ │(9004)   │     │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘     │
└──────────────────┬───────────────────────────────────┘
                   │
┌──────────────────┴───────────────────────────────────┐
│                  基础设施层                             │
│  MySQL 8.4 | Redis 7.0 | Nacos 2.2 | Nginx 1.24     │
│  (全部通过 Docker Compose 编排)                         │
└──────────────────────────────────────────────────────┘
```

### 2.2 技术栈详细说明

| 层级 | 技术 | 用途 |
|------|------|------|
| 前端 | Vue 3 + Composition API | 单页应用框架 |
| 前端 | Element Plus | UI组件库 |
| 前端 | Pinia | 状态管理（带5分钟缓存机制） |
| 前端 | Vite | 构建工具 |
| 前端 | ECharts | 填表结果图表分析 |
| 前端 | Marked + DOMPurify | Markdown安全渲染 |
| 前端 | Mammoth | Word文档前端预览 |
| 前端 | XLSX (SheetJS) | Excel文档前端预览 |
| 网关 | Spring Cloud Gateway | API路由与限流 |
| 后端 | Spring Boot 3.2 | 微服务框架 |
| 后端 | MyBatis-Plus | ORM持久化 |
| 后端 | Apache POI (XSSF/XWPF) | Excel/Word模板读写 |
| 后端 | Spring AI (DashScope) | LLM集成 |
| AI | 通义千问 (qwen-plus) | 文档信息提取与难例判定 |
| 数据库 | MySQL 8.4 | 业务数据存储 |
| 缓存 | Redis 7.0 | 分布式锁、会话缓存 |
| 注册中心 | Nacos 2.2 | 服务注册与配置管理 |
| 反向代理 | Nginx | 前端静态文件 + API代理 |
| 容器化 | Docker Compose | 中间件统一编排 |

### 2.3 为什么选择这个技术栈

1. **Vue 3 + Element Plus**：生态成熟、组件丰富，开发效率高
2. **Spring Cloud 微服务**：服务拆分清晰，各服务独立部署和扩展
3. **Apache POI**：Java 生态中最成熟的 Office 文件读写库，直接操作 xlsx/docx 二进制格式
4. **Spring AI + DashScope**：Spring 官方 AI 集成框架，与阿里云通义千问无缝对接
5. **Docker Compose**：中间件一键部署，避免环境差异问题

---

## 三、三大核心模块详解

### 3.1 模块一：文档智能编辑操作交互

**功能描述**：基于自然语言处理与文档结构理解技术，用户通过自然语言指令与 AI 对话，实现文档分析、编辑、润色、格式调整、内容提取等操作。

**技术实现**：
- 前端使用 SSE (Server-Sent Events) 实现流式响应，打字效果实时展示
- 支持关联已上传文档进行深度分析
- 支持多轮对话，对话历史持久化存储
- AI 结果可导出为 Word 文档
- 支持模型热切换（通义千问/DeepSeek等）

**API接口**：
```
POST /api/v1/ai/chat/stream  -- SSE流式AI对话
GET  /api/v1/ai/conversations -- 获取对话列表
POST /api/v1/ai/conversations -- 创建新对话
```

**关键代码设计**：
- SSE 流式传输避免长连接超时
- 5分钟超时保护机制
- 消息分为进度事件(progress)和完成事件(complete)
- 前端采用 AbortController 实现"停止生成"功能

### 3.2 模块二：非结构化文档信息提取

**功能描述**：支持 Word/Excel/TXT/Markdown 多格式文档解析，通过 AI 自动提取关键实体、数值、日期等结构化信息并存入数据库。

**技术实现**：
- 文档上传后立即返回，后台异步执行 LLM 提取
- 使用信号量(Semaphore)控制 LLM 并发数，避免 API 限流
- 带重试机制（最多3次），指数退避
- LLM 调用间隔控制（1.5秒），避免触发速率限制
- 前端轮询机制（前30秒每3秒，之后每8秒）检测提取完成状态

**提取字段规范**：
```json
{
  "fieldKey": "project_name",
  "fieldName": "项目名称",
  "fieldValue": "智能文档处理系统",
  "fieldType": "text",
  "aliases": ["课题名称"],
  "sourceText": "项目名称：智能文档处理系统",
  "confidence": 0.95
}
```

**字段标准化**：
- 内置 15+ 标准字段映射（project_name, owner, org_name, phone等）
- 日期统一为 YYYY-MM-DD
- 电话统一为纯数字
- 中文全角半角统一

**数据库表设计**：
- `source_documents` — 源文档记录
- `extracted_fields` — 提取字段
- `field_alias_dict` — 字段别名词典

### 3.3 模块三：表格自定义数据填写

**功能描述**：上传模板文件（Word/Excel），系统从已提取的文档数据库中自动搜索匹配数据并填充到模板对应位置。

**八阶段处理流程**：

| 阶段 | 名称 | 是否调用LLM | 说明 |
|------|------|------------|------|
| 0 | 基础设施 | 否 | 统一数据契约定义 |
| 1 | 源文档抽取 | 是(提取时) | 异步预提取，不在填表时执行 |
| 2 | 字段标准化 | 否 | 归一化处理 + 别名词典 |
| 3 | 模板解析 | 否 | 代码规则识别槽位 |
| 4 | 候选召回 | 否 | 五步规则召回 Top-5 |
| 5 | 难例判定 | 仅难例 | LLM从候选中选择 |
| 6 | 置信度融合 | 否 | 阈值决策 |
| 7 | 模板写回 | 否 | POI直接写回文件 |
| 8 | 结果审计 | 否 | 完整追溯日志 |

**候选召回策略（阶段4）**：
```
candidate_score = 
  0.40 * alias_match_score +    // 别名精确/模糊匹配
  0.20 * type_match_score +     // 字段类型匹配
  0.20 * source_vote_score +    // 多文档投票
  0.10 * context_match_score +  // 上下文关键字
  0.10 * field_confidence       // 提取置信度
```

**LLM难例判定条件（阶段5）**：
仅在以下条件满足时才调用LLM，否则纯规则决策：
1. Top1与Top2分差 < 0.08 且候选值不同
2. Top1分数 < 0.40 且别名完全未命中

**置信度阈值策略（阶段6）**：
- >= 0.70：直接填写（高置信）
- 0.20 ~ 0.70：填写，标记建议人工复核
- < 0.20：拒绝填写，保留空白

**Excel模板写回（Apache POI XSSF）**：
- 定位 sheet + cell 坐标
- 只改目标值，不改整张 sheet 结构
- 遇到公式格不覆盖
- 支持合并单元格处理

**Word模板写回（Apache POI XWPF）**：
- 遍历表格定位目标单元格
- 空白单元格直接写值
- 内联模式（标签:____）定点替换
- 段落级替换保留原有格式

**超时控制**：
- 全局50秒上限（留余量给写回和网络传输）
- 超时后跳过LLM调用，直接使用规则Top1结果

---

## 四、创新要点（不超过3个）

### 创新点1：源文档预提取 + 模板实时匹配的双阶段架构

传统方案是在用户上传模板时才开始读取源文档并提取信息，导致响应时间长。DocAI 将信息提取和模板填充分离：
- **上传时抽取**：源文档上传后立即异步执行结构化抽取，不阻塞用户操作
- **填表时匹配**：模板上传后只做槽位识别和候选检索，从已建好的字段库中快速匹配

这种架构使得模板填充时不需要再次调用LLM读取源文档，响应时间从分钟级降至秒级。

### 创新点2：规则优先、模型辅助的多级决策机制

系统不直接让LLM决定最终填写值，而是采用分层决策：
1. **第一层 — 规则召回**：别名匹配、类型过滤、多文档投票
2. **第二层 — LLM判定**：仅对规则无法确定的难例调用LLM
3. **第三层 — 置信度兜底**：低置信度主动拒填，避免错误填写

这种设计确保了：
- 速度快（90%以上的字段纯规则决策，无需等待LLM）
- 准确率高（LLM只处理真正需要语义理解的字段）
- 结果可解释（每个字段都有决策原因和来源追溯）

### 创新点3：全链路可追溯的审计日志系统

每个填入字段都生成完整的审计记录，包含：
- 填入值及其来源文档和原文
- 所有候选字段及评分
- 决策方式（纯规则/规则+LLM/拒填）
- 决策理由（自然语言描述）

审计日志不仅支持结果验证，还为系统优化提供数据支撑。

---

## 五、安全设计

| 安全措施 | 实现方式 |
|---------|---------|
| 身份认证 | JWT Token + Spring Security |
| 访问控制 | 每个API请求验证用户身份和权限 |
| 数据隔离 | 文档按用户ID隔离，不能访问他人文档 |
| XSS 防护 | DOMPurify 过滤HTML内容 |
| 文件验证 | 服务端校验文件类型和大小 |
| HTTPS | Nginx 支持 SSL/TLS 配置 |
| 密码安全 | 数据库存储加密密码 |

---

## 六、部署方案

### 6.1 一键部署

```bash
# Ubuntu 24.04
cd docai-pro
export DOC_DASHSCOPE_API_KEY="your-key"
bash start-lite.sh
```

自动完成：
1. 后端 Maven 构建（含 clean）
2. 前端 npm 构建
3. Docker Compose 启动 MySQL/Redis/Nacos/Nginx
4. 逐个启动 Java 微服务
5. 等待端口就绪并输出访问地址

### 6.2 服务端口

| 服务 | 端口 | 说明 |
|------|------|------|
| Nginx | 8080 | 前端入口 + API代理 |
| Gateway | 18080 | API网关 |
| 用户服务 | 9001 | 认证/用户管理 |
| AI服务 | 9002 | 文档提取/模板填充/AI对话 |
| 文件服务 | 9003 | 文件管理 |
| MySQL | 3306 | 数据存储 |
| Redis | 6379 | 缓存 |
| Nacos | 8848 | 配置中心 |

---

## 七、常见问答准备

### Q1: 为什么选择 Apache POI 而不是 ExcelJS/python-docx？

A: 我们的后端主技术栈是 Java/Spring Boot，Apache POI 是 Java 生态中最成熟的 Office 文件操作库，可以直接在微服务中使用，无需跨语言调用。POI 的 XSSF（Excel）和 XWPF（Word）组件支持完整的读写操作，性能稳定。虽然参考方案建议用 Node.js + ExcelJS 和 Python + python-docx，但我们统一使用 Java 技术栈降低了系统复杂度和部署难度。

### Q2: 如何保证信息提取的准确性？

A: 我们采用多层保障：
1. **一文档一调用**：避免多文档混在一个prompt中导致跨文档信息污染
2. **结构化输出约束**：给LLM严格的JSON schema，确保输出格式规范
3. **字段带来源标注**：每个字段都记录来源文本和位置，支持验证
4. **字段标准化**：统一别名映射和格式归一化，减少后续匹配误差
5. **重试机制**：LLM调用失败自动重试（最多3次，指数退避）

### Q3: 如何实现 60 秒内完成表格填充？

A: 关键优化：
1. **预提取架构**：源文档信息在上传时异步提取，填表时无需再次调用LLM读取源文档
2. **规则优先**：90%+的字段通过规则召回直接决策，仅难例调用LLM
3. **超时控制**：50秒全局时限，超时后跳过LLM直接使用规则结果
4. **候选Top-K**：每个槽位只保留Top-5候选，LLM只做选择题不做开放式生成

### Q4: LLM 用的是哪个模型？为什么？

A: 使用阿里云通义千问（qwen-plus）：
- 支持超长上下文（100万token），适合处理大文档
- DashScope 提供稳定的 API 服务
- 支持结构化JSON输出
- 国内部署，延迟低、合规
- 同时支持 DeepSeek 等模型热切换

### Q5: 如果 AI 提取的信息有误怎么办？

A: 系统有多层容错：
1. **置信度机制**：低置信度字段标记"建议人工复核"或直接拒填
2. **审计日志**：每个字段记录完整决策过程，便于人工检查
3. **前端决策详情**：填表结果页展示每个字段的置信度、决策方式和原因
4. **结果图表分析**：ECharts 可视化展示置信度分布、决策方式分布

### Q6: 支持哪些文档格式？

A: 源文档支持 Word(.docx)、Excel(.xlsx)、纯文本(.txt)、Markdown(.md) 四种格式。模板文件支持 Word(.docx) 和 Excel(.xlsx) 两种格式。

### Q7: 系统如何保证数据安全？

A: 
1. JWT 令牌认证，每个API请求验证身份
2. 文档按用户ID严格隔离
3. 密码加密存储
4. DOMPurify 防止 XSS 攻击
5. 服务端校验文件类型和大小，防止恶意文件上传
6. Nginx 支持 HTTPS 配置

### Q8: 前后端如何通信？

A: 
- 普通请求：RESTful API，JSON格式
- AI对话：SSE (Server-Sent Events) 流式传输
- 文件操作：multipart/form-data 上传，blob 下载
- 所有请求通过 Nginx 反向代理到 Spring Cloud Gateway，再路由到具体微服务

### Q9: 微服务之间如何通信？

A: 当前阶段各微服务独立运行，通过 Gateway 统一入口路由。服务间无直接RPC调用，通过数据库共享数据。后续可升级为 Feign/OpenFeign 进行服务间调用。

### Q10: 模板槽位是如何识别的？

A: 完全使用代码规则识别（不调用LLM），三种模式：
1. **左标签右空白**：标签在左边，右边单元格为空
2. **上标签下空白**：标签在上面，下面单元格为空
3. **内联标签**：单元格内 "字段名：____" 格式

Excel 使用 Apache POI XSSF 遍历所有Sheet和单元格。
Word 使用 Apache POI XWPF 遍历所有表格、行、列。

### Q11: 系统的并发能力如何？

A: 
- Nginx 支持 10000 并发连接
- Spring Cloud Gateway 负载均衡
- LLM 调用使用信号量控制并发（避免API限流）
- MySQL 最大连接数 2000
- Redis 用于分布式锁和缓存
- 文档提取线程池（4线程并行）

### Q12: 候选字段召回的五步策略具体是什么？

A:
1. **精确别名匹配**（权重0.40）：槽位标签与字段名或别名词典精确匹配
2. **类型匹配**（权重0.20）：槽位期望类型与字段类型一致
3. **多文档投票**（权重0.20）：同一值在多个文档中出现则得分更高
4. **上下文关键字加权**（权重0.10）：槽位上下文包含相关关键字
5. **字段置信度**（权重0.10）：原始提取置信度

---

## 八、项目数据库设计

### 核心表

| 表名 | 说明 |
|------|------|
| users | 用户信息 |
| source_documents | 源文档记录 |
| extracted_fields | 提取的结构化字段 |
| field_alias_dict | 字段别名词典 |
| template_files | 模板文件记录 |
| template_slots | 模板槽位 |
| fill_decisions | 填充决策详情 |
| fill_audit_logs | 填充审计日志 |
| conversations | AI对话会话 |
| conversation_messages | 对话消息 |

---

## 九、测试与验证

### 9.1 测试方案

按比赛评价方法：
1. 上传测试文档样本集（5个docx + 3个md + 5个xlsx + 3个txt）
2. 逐个上传5个模板表格文件
3. 每个模板执行自动填表
4. 记录响应时间和准确率

### 9.2 性能优化措施

| 优化项 | 具体措施 |
|--------|---------|
| 源文档预提取 | 上传时异步提取，填表时零等待 |
| 规则优先 | 90%+字段纯规则决策 |
| LLM调用最小化 | 仅难例调用，超时直接跳过 |
| 全局超时控制 | 50秒上限 |
| 前端缓存 | 文档列表5分钟缓存 |

---

## 十、项目成员与分工

（根据团队实际情况填写）

| 成员 | 负责模块 |
|------|---------|
| - | 前端开发（Vue 3 + Element Plus） |
| - | 后端 AI 服务（信息提取 + 模板填充） |
| - | 后端用户/文件服务 + 部署运维 |
| - | 测试验证 + 文档撰写 |

---

## 十一、项目目录结构

```
DocAI/
├── docai-frontend/          # 前端项目
│   ├── src/
│   │   ├── views/           # 页面组件
│   │   ├── api/             # API 接口
│   │   ├── store/           # 状态管理
│   │   ├── router/          # 路由
│   │   └── layout/          # 布局
│   └── vite.config.js       # 构建配置
├── docai-pro/               # 后端项目
│   ├── ai-service/          # AI 服务（核心）
│   ├── user-service/        # 用户服务
│   ├── file-service/        # 文件服务
│   ├── gateway-service/     # 网关服务
│   ├── common-service/      # 公共模块
│   ├── mcp-service/         # MCP 服务
│   ├── deploy/              # 部署配置
│   │   ├── docker-compose-*.yml
│   │   ├── nginx/           # Nginx 配置
│   │   ├── mysql/           # 数据库初始化
│   │   └── redis/           # Redis 配置
│   ├── start-lite.sh        # Linux一键启动
│   └── start-lite-windows.ps1  # Windows一键启动
├── docs/                    # 项目文档
└── test-data/               # 测试数据
```

---

*DocAI 智能文档处理系统 — 让 AI 赋能每一份文档*
