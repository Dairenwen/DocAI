# DocAI 项目详细流程介绍

## 一、系统全局架构流程图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户浏览器                                      │
│                         http://localhost:8080                                │
└─────────────────────────────────┬───────────────────────────────────────────┘
                                  │ HTTP / SSE
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    Nginx 反向代理 (Port 8080)                                │
│  ┌───────────────────────────┐  ┌────────────────────────────────────────┐  │
│  │  静态资源 /               │  │  API代理 /api/v1/** → Gateway:18080   │  │
│  │  Vue3 SPA (dist/)        │  │  超时300s, 支持SSE流式传输              │  │
│  └───────────────────────────┘  └────────────────────────────────────────┘  │
└─────────────────────────────────┬───────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                  Spring Cloud Gateway (Port 18080)                           │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │  轻量模式: 仅路由转发，JWT认证由各微服务自行校验                          │ │
│  │  完整模式: Gateway级JWT认证过滤器统一校验                                │ │
│  │  白名单: /api/v1/users/auth, /api/v1/users/verification-code          │ │
│  │  响应超时: 300s, 连接超时: 10s                                         │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│  路由规则 (StripPrefix=2: 去掉 /api/v1 前缀):                               │
│  ┌──────────────────┬──────────────────────────────────────────────────┐    │
│  │ /api/v1/users/** │ → user-service (127.0.0.1:9001) /users/**      │    │
│  │ /api/v1/files/** │ → file-service (127.0.0.1:9003) /files/**      │    │
│  │ /api/v1/ai/**    │ → ai-service   (127.0.0.1:9002) /ai/**        │    │
│  │ /api/v1/llm/**   │ → ai-service   (127.0.0.1:9002) /llm/**       │    │
│  │ /api/v1/source/**│ → ai-service   (127.0.0.1:9002) /source/**    │    │
│  │ /api/v1/template/**│→ ai-service  (127.0.0.1:9002) /template/**  │    │
│  └──────────────────┴──────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、前端模块详细流程

### 2.1 前端页面结构
```
App.vue (根组件)
├── Login.vue          ── 登录/注册页面
└── MainLayout.vue     ── 主布局框架（需认证）
    ├── Dashboard.vue     ── 工作台首页
    ├── DocumentList.vue  ── 文档管理
    ├── AutoFill.vue      ── 智能填表
    ├── AIChat.vue        ── AI对话
    └── AIGenerate.vue    ── AI写作
```

### 2.2 登录认证流程
```
用户输入用户名+密码
        │
        ▼
POST /api/v1/users/auth
        │
        ▼
┌──────────────────┐
│ user-service     │
│ UserController   │
│ .auth()          │
│   │              │
│   ▼              │
│ UserServiceImpl  │
│ .auth()          │
│   │              │
│   ├─ BCrypt验证密码（失败返回400，提示"用户名与密码不匹配"）
│   ├─ JwtUtil生成Token (HS256, 20h有效期)
│   └─ Redis存储Token（key: token:<jwt>）
└──────────────────┘
        │
        ▼
返回 { token, userId, username }
        │
        ▼
前端 localStorage 存储 token
```

### 2.3 文档上传与管理流程
```
用户在DocumentList.vue选择文件（支持docx/xlsx/txt/md四种格式）
        │
        ▼
POST /api/v1/source/upload (multipart/form-data)
        │
        ├─ 传输阶段 (进度0%-50%)
        │   uploadQueue进度条实时更新
        │
        ▼
┌─────────────────────────────────────────────────────┐
│ ai-service > ExtractionController.uploadAndExtract() │
│                                                      │
│  1. 文件保存到本地临时目录                              │
│  2. 上传至OSS (可选)                                  │
│  3. 创建source_documents记录 (status=parsing)         │
│  4. 调用 extractTextContent() 读取文件文本             │
│  5. 调用 callLlmExtract() 发送给LLM抽取               │
│  6. 解析LLM返回的JSON字段列表                          │
│  7. 标准化字段 + 存入extracted_fields表                │
│  8. 更新别名词典field_alias_dict                       │
│  9. 更新文档状态为 parsed                              │
└─────────────────────────────────────────────────────┘
        │
        ├─ 抽取阶段 (进度50%-100%)
        │  (完成/失败后2秒自动隐藏进度浮窗)
        │
        ▼
Pinia documentStore 刷新文档列表

智能轮询机制：
├── 上传后自动启用轻量级状态轮询（GET /source/documents/status）
├── 自适应间隔：前10次每3秒，之后每8秒
├── 最大轮询120次（约10分钟自动停止）
├── 检测到状态变化时才刷新完整文档列表
└── 全部文档解析完成后自动停止轮询

文档管理操作：
├── 文件预览：下载原文件在前端预览（txt/md直接显示，docx用mammoth渲染，xlsx用SheetJS渲染）
├── AI对话：关联源文档进入AI智能对话（需提取成功）
├── 下载：下载用户原始上传文件
├── 删除：删除源文档及关联的提取字段
├── 批量下载：勾选多个文档后一键批量下载
└── 批量删除：勾选多个文档后一键批量删除（调用 DELETE /source/batch）
```

---

## 三、后端微服务模块详解

### 3.1 用户服务 (user-service, Port 9001)

```
                    UserController
                    ├── POST /auth              → 登录认证
                    ├── POST /verification-code  → 发送验证码
                    ├── GET  /info              → 获取用户信息
                    ├── POST /change-password    → 修改密码
                    ├── POST /password/reset-by-email → 重置密码
                    └── POST /logout            → 退出登录

依赖链：
UserController → UserServiceImpl → UserMapper (MyBatis-Plus)
                                 → JwtUtil (Token管理)
                                 → RedisService (Token/验证码存储)
                                 → VerificationCodeService (6位验证码)
                                 → EmailService (发送验证码邮件)
```

### 3.2 文件服务 (file-service, Port 9003)

```
                    FilesController
                    ├── POST /files/upload/single → 上传Excel文件
                    ├── GET  /files/list         → 文件列表(分页)
                    ├── GET  /files/download      → 下载文件
                    ├── GET  /files/excel/preview/{id} → Excel预览(分页)
                    ├── GET  /files/excel/info/{id}    → Excel信息
                    ├── POST /files/restore/{id}       → 从OSS恢复数据
                    └── POST /files/delete              → 删除文件

核心流程（Excel上传）：
1. FileServiceImpl.upload()
   ├── 文件校验 (格式、大小50MB限制)
   ├── 上传至OSS
   ├── 创建files记录
   └── 调用ExcelToTableService
       ├── 读取Excel所有Sheet
       ├── 为每个Sheet动态建MySQL表
       ├── 批量插入数据 (1000行/批)
       ├── 记录file_table_mappings
       └── 记录field_mappings
```

### 3.3 AI服务 (ai-service, Port 9002)

这是项目核心服务，包含4个Controller：

```
┌─────────────────────────────────────────────────────────────┐
│                   AI-SERVICE (Port 9002)                    │
│                                                             │
│  ExtractionController      ── 源文档抽取 (阶段1-2)            │
│  ├── POST /source/upload   → 上传并抽取                      │
│  ├── GET  /source/{id}/fields → 查看抽取字段                  │
│  ├── GET  /source/documents  → 文档列表                      │
│  ├── GET  /source/documents/status → 轻量状态轮询             │
│  ├── GET  /source/{id}      → 文档详情                       │
│  ├── GET  /source/{id}/download → 下载原文档                  │
│  ├── DELETE /source/{id}    → 删除文档                       │
│  └── DELETE /source/batch   → 批量删除文档                    │
│                                                             │
│  TemplateFillController    ── 模板填写 (阶段3-8)              │
│  ├── POST /template/upload  → 上传模板                       │
│  ├── POST /template/{id}/parse → 解析槽位                    │
│  ├── POST /template/{id}/fill  → 自动填表                    │
│  ├── GET  /template/{id}/audit → 审计日志                    │
│  ├── GET  /template/{id}/decisions → 决策详情                │
│  ├── GET  /template/list    → 模板列表                       │
│  ├── GET  /template/{id}/download → 下载结果                 │
│  └── POST /template/{id}/send-email → 发送结果至邮箱         │
│                                                             │
│  AiController              ── AI对话                         │
│  ├── GET  /ai/chat/stream  → SSE流式对话                     │
│  ├── GET  /ai/requests     → 请求历史                        │
│  └── POST /ai/send-email   → 邮件发送                        │
│                                                             │
│  LlmController             ── 模型管理                       │
│  ├── GET  /llm/providers/list    → 模型列表                  │
│  ├── POST /llm/providers/switch  → 切换模型                  │
│  └── GET  /llm/providers/current → 当前模型                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、8阶段智能填表核心流程

### 阶段0：统一数据契约
```
6个核心数据结构：
┌─────────────────┐  ┌──────────────────┐  ┌──────────────┐
│ SourceDocument  │  │ ExtractedField   │  │ TemplateSlot │
│ ─ docId         │  │ ─ fieldId        │  │ ─ slotId     │
│ ─ fileName      │  │ ─ fieldKey       │  │ ─ label      │
│ ─ fileType      │  │ ─ fieldName      │  │ ─ context    │
│ ─ uploadStatus  │  │ ─ fieldValue     │  │ ─ position   │
└─────────────────┘  │ ─ fieldType      │  │ ─ expectedType│
                     │ ─ confidence     │  └──────────────┘
                     │ ─ sourceText     │
                     └──────────────────┘
┌─────────────────┐  ┌──────────────────┐  ┌──────────────┐
│ FillCandidate   │  │ FillDecision     │  │ FillAuditLog │
│ ─ slotId        │  │ ─ finalValue     │  │ ─ auditId    │
│ ─ fieldId       │  │ ─ finalConfidence│  │ ─ sourceDoc  │
│ ─ score         │  │ ─ decisionMode   │  │ ─ sourceText │
│ ─ matchReasons  │  │ ─ reason         │  │ ─ reason     │
└─────────────────┘  └──────────────────┘  └──────────────┘
```

### 阶段1：源文档结构化抽取
```
MultipartFile(源文档)
        │
        ▼
┌─ extractTextContent() ─────────────────────────────┐
│  ├─ docx → Apache POI XWPFDocument                 │
│  │         段落 + 表格 → 纯文本                      │
│  ├─ xlsx → Apache POI XSSFWorkbook                 │
│  │         逐Sheet逐行 → 纯文本                      │
│  ├─ txt  → Files.readString()                      │
│  └─ md   → Files.readString()                      │
└────────────────────────────────────────────────────┘
        │ 纯文本内容
        ▼
┌─ callLlmExtract() ─────────────────────────────────┐
│  构建增强版抽取Prompt (含文件名、文档内容、输出Schema) │
│  Prompt覆盖12大类信息实体：                          │
│    项目信息/人员信息/机构信息/联系方式/证件信息/      │
│    时间信息/经费信息/研究信息/地理信息/数量统计/      │
│    非结构化实体/表格数据                              │
│  最大文本长度限制: 30000字符                          │
│                                                     │
│  ★ 多模态图像识别 (docx文档)：                       │
│    extractImagesFromDocx() 提取文档内嵌图片           │
│    图片转Base64编码，随文本一起发送给VL模型            │
│    限制: 最多2张图片，每张≤10MB，跳过<1KB小图         │
│    VL模型可识别图片中的表格、文字、图表等信息          │
│        │                                            │
│        ▼                                            │
│  LlmService.generateMultiModalText(prompt, images)  │
│  → DashScopeProvider → 阿里云 qwen2.5-vl-7b-instruct │
│        │                                            │
│        ▼                                            │
│  解析返回的JSON                                      │
│  { fields: [ {field_key, field_name,                │
│                field_value, field_type,             │
│                source_text, confidence} ] }         │
│                                                     │
│  若LLM失败 → ruleBasedExtract() 增强版规则引擎兜底   │
│  规则1: "键：值" 模式匹配                             │
│  规则2: Markdown表格解析（支持---分隔行，自动查找     │
│         真正的表头行，逐行逐列多值提取）              │
│  规则3: 行内实体提取（手机号/邮箱/身份证号）          │
│                                                     │
│  关键策略: 同类字段多次出现时逐一独立提取              │
│  （如3个国家→3条country记录, 5个省份→5条记录）        │
└─────────────────────────────────────────────────────┘
        │ List<ExtractedFieldEntity>
        ▼
数据库: extracted_fields 表
```

### 阶段2：字段标准化与别名词典
```
ExtractedFieldEntity
        │
        ▼
┌─ standardizeField() ───────────────────────────────┐
│  1. cleanFieldName(): 去除冒号、括号、换行            │
│  2. normalizeFieldKey(): 映射到标准字段键             │
│     已覆盖40+种中文字段名到30+个标准key：              │
│     project_name, owner, org_name, phone, email,    │
│     id_number, address, start_date, end_date,       │
│     budget, dept_name, title, degree,               │
│     research_field, postal_code,                    │
│     project_type, project_number, project_level,    │
│     gender, age, birth_date, apply_date,            │
│     fax, keywords, discipline, funding_amount,      │
│     contact_person, id_card,                        │
│     population, country, state_province, area_size, │
│     capital, gdp, language, currency, region, city, │
│     description, quantity 等                         │
│  3. 值格式化:                                       │
│     日期 → YYYY-MM-DD                               │
│     电话 → 纯数字字符串                              │
│  4. updateAliasDict(): 更新别名词典                   │
└─────────────────────────────────────────────────────┘
        │
        ▼
数据库: field_alias_dict 表
┌──────────────┬──────────────────────────┐
│ standard_key │ alias_name               │
├──────────────┼──────────────────────────┤
│ project_name │ 项目名称                  │
│ project_name │ 课题名称                  │
│ project_name │ 申报项目名称              │
│ owner        │ 负责人                    │
│ owner        │ 项目负责人                │
│ phone        │ 联系电话                  │
│ phone        │ 手机号码                  │
└──────────────┴──────────────────────────┘
```

### 阶段3：模板槽位识别
```
模板文件 (xlsx/docx)
        │
        ▼
┌─ parseSlots() ──────────────────────────────────────┐
│                                                     │
│  xlsx → parseExcelSlots()                           │
│  ├─ 遍历所有Sheet                                    │
│  ├─ 遍历每个单元格                                    │
│  ├─ isLabelCell(): 判断是否为标签                     │
│  │   (长度2-20字、非纯数字、非说明文字)                 │
│  └─ 三类槽位识别:                                     │
│     ┌─────────────────────────────────────────┐      │
│     │规则1: 左边非空、右边空 → 右边是槽位         │      │
│     │  [项目名称] [______]                     │      │
│     │                                         │      │
│     │规则2: 上边非空、下边空 → 下边是槽位         │      │
│     │  [项目名称]                              │      │
│     │  [______]                               │      │
│     │                                         │      │
│     │规则3: 同单元格 "标签：____" → 内联槽位     │      │
│     │  [项目名称：________]                    │      │
│     └─────────────────────────────────────────┘      │
│                                                      │
│  docx → parseWordSlots()                             │
│  ├─ 遍历所有表格                                      │
│  ├─ 遍历每行每列                                      │
│  └─ 同样的三类槽位识别规则                             │
│                                                      │
│  统一输出: TemplateSlotEntity                         │
│  { label, context, position(JSON), expectedType }    │
└──────────────────────────────────────────────────────┘
        │
        ▼
数据库: template_slots 表
```

### 阶段3.5（增强）：多阶段表头引导提取
```
根据模板表头标签，通过多种手段从源文档中精准提取缺失字段
特别适用于非结构化文档（md/txt/docx），支持多源文档混合提取

模板槽位列表 + 现有提取字段
        │
        ▼
┌─ headerGuidedExtraction() ──────────────────────────┐
│  步骤0: 对比槽位标签与已有字段，找出尚未匹配的槽位      │
│                                                      │
│  步骤1: docx表格直接提取 (extractFieldsFromDocxTables)  │
│     对源docx文件，直接打开并解析其表格结构               │
│     ◆ 多行数据表格检测: 识别表头行→逐行逐列提取数据     │
│       (如表头"国家"|"省份"→从每行提取所有国家和省份)     │
│     ◆ KV行对扫描: 检测key-value行对（标签+值）          │
│       复合标签允许多个不同值（不再跳过已匹配标签）       │
│     使用别名映射扩展匹配（30+种中文别名/标准key）       │
│     准确率最高（0.88~0.90），直接利用表格结构化信息      │
│                                                      │
│  步骤2: 正则全量提取                                   │
│     在源文档文本中搜索 "标签：值" 或 "标签: 值" 模式     │
│     搜索词包含标签本身 + 同标准key下的所有别名           │
│     ◆ 复合标签全量匹配: 提取所有出现的匹配项            │
│       (不再仅取第一个)，每个子标签使用独立标准化key      │
│     置信度 0.85，无需LLM调用，速度最快                  │
│                                                      │
│  步骤3: LLM上下文精准提取                              │
│     对仍缺失的标签，搜索源文档中的上下文                 │
│     extractContextForLabel(): 前后各500字符上下文       │
│     搜索策略: 完整标签 → 别名词汇 → 核心词              │
│     将上下文发送LLM进行精准JSON格式提取                  │
│     ◆ 复合标签提示: 告知LLM分别提取"/"两侧所有值       │
│     ◆ 多行LLM响应自动拆分为独立字段便于后续匹配         │
│                                                      │
│  关键改进（相比旧版本）:                               │
│  - 新增docx表格直接解析，不再仅依赖文本搜索             │
│  - 正则提取先于LLM调用，减少API开销                    │
│  - 上下文窗口从200→500字符，提取更完整信息              │
│  - 别名扩展搜索：每个标签自动扩展20+中文同义词          │
│  - 多源文档：跨docx/xlsx/txt/md综合提取数据             │
│  - 容错高：每个步骤独立，异常不影响后续步骤              │
└──────────────────────────────────────────────────────┘
        │
        ▼
补充字段合并到 allFields 列表中
```

### 阶段4：候选字段召回
```
对每个TemplateSlot，从extracted_fields中召回Top-5候选：

TemplateSlot(label="项目名称")
        │
        ▼
┌─ recallCandidates() ────────────────────────────────┐
│  对每个ExtractedField计算6维得分:                      │
│                                                      │
│  1. alias_match_score (权重0.34)                     │
│     ├─ 精确匹配 label==fieldName → 1.0              │
│     ├─ 别名词典匹配 → 0.9                            │
│     ├─ 模糊包含匹配 → 0.5~0.7                       │
│     ├─ 字符重叠匹配(≥3字符且≥60%重叠) → 0.4         │
│     ├─ 短标签(≤2字符)禁用字符重叠，仅精确/包含匹配   │
│     └─ 无匹配 → 0.0                                 │
│                                                      │
│  2. type_match_score (权重0.18)                      │
│     ├─ 类型完全匹配 → 1.0                            │
│     ├─ text类型兼容 → 0.3                            │
│     └─ 类型冲突 → 0.0                               │
│                                                      │
│  2.5 value_type_validation (交叉验证，惩罚项)         │
│     ├─ 数字槽位但值无数字 → -0.3                     │
│     ├─ 日期槽位但值无年份 → -0.3                     │
│     ├─ 电话槽位但值少于5位数字 → -0.3                │
│     └─ 人员槽位但值全为数字 → -0.3                   │
│                                                      │
│  2.6 composite_label_match (复合标签匹配)             │
│     ├─ 标签含"/"时拆分为子标签分别匹配                │
│     ├─ 各子项通过normalizeLabel标准化 → 0.55~0.85    │
│     └─ 收集所有匹配值，分行写入不同行                  │
│                                                      │
│  2.7 resolveCompositeLabel (复合标签全量匹配)         │
│     ├─ 合并allFields与allFieldsRaw两个字段列表        │
│     ├─ 6层匹配: key精确→名称精确→别名集合→包含→      │
│     │  别名双向交叉→字段自带aliases匹配               │
│     ├─ 多值字段(含逗号/顿号/换行)自动拆分             │
│     └─ 只要找到任意值即返回（不再要求≥2个）           │
│                                                      │
│  3. source_vote_score (权重0.16)                     │
│     └─ 多文档出现相同值 → votes/maxVote              │
│                                                      │
│  4. context_match_score (权重0.10)                   │
│     └─ 上下文关键词匹配 → 0.8                        │
│                                                      │
│  5. field_confidence (权重0.08)                      │
│     └─ LLM抽取时的置信度                             │
│                                                      │
│  6. requirement_match_score (权重0.14)               │
│     ├─ 命中用户需求关键词(时间/地点/日期/类型) → 加分   │
│     └─ 完全不命中需求关键词 → 显著降权                 │
│                                                      │
│  candidate_score = 0.34*alias + 0.18*type            │
│                  + 0.16*vote + 0.10*context           │
│                  + 0.08*confidence + 0.14*requirement │
│                                                      │
│  排序取Top-5；有需求时放宽召回阈值，再由需求匹配二次排序  │
└──────────────────────────────────────────────────────┘
        │ List<CandidateResult> (Top-5)
        ▼
```

### 阶段5：难例判定（LLM）
```
                 Top-5 候选
                     │
                     ▼
┌─ makeDecision() ────────────────────────────────────┐
│                                                      │
│  是否需要调用LLM？(满足任一条件即调用)                  │
│  ┌─────────────────────────────────────────────┐     │
│  │ 1. Top1 - Top2 分差 < 0.15                  │     │
│  │ 2. Top1 分数 < 0.75                         │     │
│  │ 3. 候选值类型冲突                            │     │
│  │ 4. 多文档给出不同值且分差<0.20               │     │
│  │ 5. 别名词典命中分 < 0.5                      │     │
│  └─────────────────────────────────────────────┘     │
│                                                      │
│  若需要 → callLlmJudge()                             │
│  ├─ 构建Prompt (仅传slot信息 + Top-5候选及sourceText) │
│  ├─ LlmService.generateText() → qwen-plus            │
│  ├─ 解析JSON响应                                     │
│  │   { selectedFieldId, selectedValue,               │
│  │     modelConfidence, reason }                     │
│  └─ 若modelConfidence < 0.65 → 拒填                  │
│                                                      │
│  若不需要 → 直接取Top-1候选                           │
└──────────────────────────────────────────────────────┘
```

### 阶段6：置信度融合与拒填
```

### 阶段6.5：强兜底非空回填
```
若仍存在空白槽位，系统会启用 requirement_force_fill / mandatory_fallback：
1. 优先使用“需求感知得分最高”的候选值回填
2. 若候选列表为空，则按字段标签字符重叠度选择最接近且有效的字段值
3. 仅在数据源本身确实缺失可用值时保留空白
```
               FillDecisionEntity
                     │
                     ▼
┌─ applyConfidenceThreshold() ─────────────────────────┐
│                                                       │
│  final_confidence 计算:                               │
│  ├─ 若调用了LLM:                                     │
│  │   = 0.70 * candidate_score + 0.30 * model_conf    │
│  └─ 若纯规则:                                        │
│      = candidate_score                                │
│                                                       │
│  决策阈值:                                            │
│  ┌───────────────────────────────────────────────┐   │
│  │ ≥ 0.70  → ✅ 直接填写（高置信）                │   │
│  │ 0.20~0.70 → ⚠️ 填写，标记建议人工复核         │   │
│  │ < 0.20  → ❌ 拒绝填写，保留空白               │   │
│  └───────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────┘
```

### 阶段7：模板写回
```
               FillDecisions + 原始模板文件
                          │
                          ▼
┌─ writeBack() ────────────────────────────────────────┐
│                                                       │
│  xlsx → writeBackExcel()                              │
│  ├─ 打开原始模板workbook                              │
│  ├─ header_below槽位 → directTableCopy()              │
│  │   ├─ 匹配模板表头标签与源Excel表头                  │
│  │   ├─ 逐行复制数据（跳过空行）                       │
│  │   ├─ 批量填充数据行区域                             │
│  │   ├─ OOM保护: 单源文件失败不影响整体                 │
│  │   └─ 更新fill_decisions表（已复制N行/置信度0.95）    │
│  ├─ 统计聚合槽位 → computeColumnAggregation()          │
│  │   ├─ 检测adjacent_blank标签匹配header_below列       │
│  │   ├─ 关键词识别聚合类型(SUM/AVG/MAX/MIN/COUNT)      │
│  │   └─ 从源Excel读取列数据并计算统计值                 │
│  ├─ 根据slot.position定位单元格                       │
│  ├─ 跳过公式格(CellType.FORMULA)                     │
│  ├─ inline模式: 追加值到现有文本后                     │
│  ├─ 普通模式: 直接写入值                              │
│  └─ 写入 _filled.xlsx 文件                            │
│                                                       │
│  docx → writeBackWord()                               │
│  ├─ 打开原始模板document                              │
│  ├─ 根据slot.position定位表格单元格                    │
│  ├─ inline模式: 替换冒号后的文本                       │
│  ├─ 普通模式: 在空白单元格写入值                       │
│  ├─ 尽量保留原格式(操作Run而非整段替换)                 │
│  └─ 写入 _filled.docx 文件                            │
│                                                       │
│  更新 template_files.output_path                      │
└───────────────────────────────────────────────────────┘
```

### 阶段8：结果审计
```
               每个填写决策
                    │
                    ▼
┌─ createAuditLog() ───────────────────────────────────┐
│  FillAuditLogEntity:                                  │
│  ├─ auditId: 审计批次ID                               │
│  ├─ templateId: 模板ID                                │
│  ├─ slotLabel: "项目名称"                              │
│  ├─ finalValue: "智能文档处理系统"                      │
│  ├─ finalConfidence: 0.91                             │
│  ├─ decisionMode: "rule_plus_llm"                     │
│  ├─ sourceDocName: "doc_01.docx"                      │
│  ├─ sourceText: "项目名称：智能文档处理系统"             │
│  ├─ reason: "规则别名命中+LLM选择该值"                  │
│  └─ candidatesSummary: "[智能文档...(0.92),...]"       │
└───────────────────────────────────────────────────────┘
        │
        ▼
数据库: fill_audit_logs + fill_decisions 表
```

---

## 五、AI对话模块流程

### 5.1 对话会话管理

对话记录通过服务端持久化存储（MySQL），支持跨浏览器/设备访问：
- 会话列表管理：`GET/POST/PUT/DELETE /api/v1/ai/conversations`
- 消息管理：`GET/POST /api/v1/ai/conversations/{id}/messages`
- 会话时间仅在发送/接收消息时更新（addMessage 时触发 updatedAt 更新）

### 5.2 AI流式对话流程

```
用户输入 "查询价格大于100的商品"
        │
        ▼
POST /api/v1/ai/chat/stream  (JSON body)
        │
        ▼
┌─ AiServiceImpl.streamUnifiedChat() ──────────────────┐
│                                                       │
│  1. SSE连接建立                                       │
│  2. 判断请求类型:                                     │
│     ├─ 无关联文件 → handleGeneralChatFlow()           │
│     │   └─ 直接调用LLM对话                            │
│     ├─ 查询类 → handleChatFlow()                      │
│     │   ├─ 获取文件关联的MySQL表结构                   │
│     │   ├─ AiModelService.getSql() → LLM生成SQL       │
│     │   ├─ SQLGenerationService.executeQuery()        │
│     │   └─ 格式化结果返回                              │
│     ├─ 修改类 → handleModificationChatFlow()          │
│     │   ├─ LLM生成UPDATE SQL                          │
│     │   ├─ 执行修改                                   │
│     │   └─ 返回修改后的Excel下载链接                    │
│     └─ 图表类 → handleChartFlow()                     │
│         ├─ 查询数据                                   │
│         ├─ 生成ECharts配置                             │
│         └─ 返回图表JSON                               │
│                                                       │
│  3. SSE事件流:                                        │
│     data: {"stage": "ANALYZING", "message": "..."}    │
│     data: {"stage": "GENERATING_SQL", ...}            │
│     data: {"stage": "SUCCESS", "result": {...}}       │
└───────────────────────────────────────────────────────┘
```

### 5.3 停止生成功能

前端支持在两个阶段中止AI生成：
1. **后端请求阶段**（loading状态）：通过 AbortController 中止 SSE fetch 请求
2. **打字动画阶段**（isStreaming状态）：停止定时器，立即展示已接收的完整内容

停止按钮替代发送按钮显示，点击后立即恢复输入状态。

### 5.4 AI文档在线编辑与导出

用户在对话中提出文档修改需求（如"把标题改为XXX"、"删除第三段"、"润色全文"），系统自动识别编辑意图并执行：

```
用户输入 "将标题修改为《2024年度报告》"
        │
        ▼
handleSourceDocChatFlow()
        │
        ├── 1. 编辑意图检测（关键词匹配）:
        │   修改/编辑/删除/增加/添加/替换/改为/改成/更新/移除/插入/润色
        │
        ├── 2. 读取源文档全文内容
        │
        ├── 3. 构建编辑Prompt → LLM返回修改后完整文档
        │
        ├── 4. 自动保存修改后文档:
        │   ├── .docx → XWPFDocument 生成Word
        │   ├── .md   → Files.writeString
        │   └── .txt  → Files.writeString
        │   保存路径: {tmpdir}/docai_edited/{原文件名_edited.ext}
        │
        ├── 5. 返回SSE响应:
        │   {isModificationRequest: true, modifiedExcelUrl: 下载URL}
        │
        └── 6. 前端弹出下载链接，用户点击即可导出
```

**API接口**：
- `POST /api/v1/ai/documents/{docId}/apply-edit` — 手动触发编辑保存
- `GET /api/v1/ai/documents/edited/{fileName}` — 下载修改后的文档

**安全措施**：文件名校验禁止路径遍历（`..`、`/`、`\`等非法字符）。

---

## 六、数据库ER关系图

```
┌─────────────┐     ┌──────────────────┐     ┌──────────────────┐
│   users     │     │     files        │     │file_table_mappings│
│ ─ id (PK)   │◄─── ┤ ─ user_id (FK)   │◄─── ┤ ─ file_id (FK)   │
│ ─ username  │     │ ─ file_name      │     │ ─ table_name     │
│ ─ email     │     │ ─ file_path      │     │ ─ sheet_index    │
│ ─ password  │     │ ─ oss_key        │     │ ─ sheet_name     │
└──────┬──────┘     └──────────────────┘     └──────────────────┘
       │                                             │
       │            ┌──────────────────┐     ┌───────▼──────────┐
       │            │  ai_requests     │     │  field_mappings  │
       │            │ ─ user_id (FK)   │     │ ─ file_id        │
       │            │ ─ file_id (FK)   │     │ ─ table_name     │
       │            │ ─ user_input     │     │ ─ db_field_name  │
       │            │ ─ ai_response    │     │ ─ original_header│
       │            └──────────────────┘     └──────────────────┘
       │
       │  ┌──────────────────┐   ┌──────────────────┐
       ├─►│source_documents  │──►│ extracted_fields │
       │  │ ─ user_id        │   │ ─ doc_id (FK)    │
       │  │ ─ file_name      │   │ ─ field_key      │
       │  │ ─ file_type      │   │ ─ field_name     │
       │  │ ─ upload_status  │   │ ─ field_value    │
       │  └──────────────────┘   │ ─ field_type     │
       │                         │ ─ confidence     │
       │  ┌──────────────────┐   │ ─ source_text    │
       │  │field_alias_dict  │   └──────────────────┘
       │  │ ─ standard_key   │
       │  │ ─ alias_name     │
       │  │ ─ field_type     │
       │  └──────────────────┘
       │
       │  ┌──────────────────┐   ┌──────────────────┐
       ├─►│ template_files   │──►│ template_slots   │
       │  │ ─ user_id        │   │ ─ template_id    │
       │  │ ─ file_name      │   │ ─ label          │
       │  │ ─ template_type  │   │ ─ context        │
       │  │ ─ parse_status   │   │ ─ position       │
       │  │ ─ output_path    │   │ ─ expected_type  │
       │  └────────┬─────────┘   └──────────────────┘
       │           │
       │  ┌────────▼─────────┐   ┌──────────────────┐
       │  │ fill_decisions   │   │ fill_audit_logs  │
       │  │ ─ template_id    │   │ ─ template_id    │
       │  │ ─ audit_id       │   │ ─ user_id        │
       │  │ ─ slot_id        │   │ ─ audit_id       │
       │  │ ─ final_value    │   │ ─ final_value    │
       │  │ ─ decision_mode  │   │ ─ source_doc_name│
       │  │ ─ confidence     │   │ ─ source_text    │
       │  └──────────────────┘   │ ─ reason         │
       │                         └──────────────────┘
       │
       │  ┌──────────────────────┐   ┌──────────────────┐
       ├─►│ chat_conversations   │──►│ chat_messages    │
       │  │ ─ user_id            │   │ ─ conversation_id│
       │  │ ─ title              │   │ ─ role           │
       │  │ ─ linked_doc_id      │   │ ─ content        │
       │  │ ─ pinned             │   │ ─ created_at     │
       │  │ ─ created_at         │   └──────────────────┘
       │  │ ─ updated_at         │
       │  └──────────────────────┘

总计14张核心表
```

---

## 七、LLM调用链路

```
┌──────────────────────────────────────────────────────────────┐
│                    LLM调用架构                                │
│                                                              │
│  LlmService (接口)                                           │
│      │                                                       │
│      ▼                                                       │
│  LlmServiceImpl                                              │
│      │  currentProvider = "dashscope"                         │
│      ▼                                                       │
│  LlmProviderFactory                                          │
│      │  providerMap: { "dashscope": DashScopeProvider,       │
│      │                  "deepseek": DeepSeekProvider }        │
│      ▼                                                       │
│  ┌────────────────────┐  ┌─────────────────────┐            │
│  │ DashScopeProvider  │  │ DeepSeekProvider    │            │
│  │ ├─ ChatClient Bean │  │ ├─ HTTP RestTemplate│            │
│  │ ├─ qwen-plus模型   │  │ └─ deepseek-chat   │            │
│  │ └─ qwen2.5-vl-7b   │                                    │
│  │   (多模态VL模型)    │                                    │
│  └────────┬───────────┘  └─────────────────────┘            │
│           │                                                  │
│           ▼                                                  │
│  阿里云DashScope API (OpenAI兼容模式)                         │
│  https://dashscope.aliyuncs.com/compatible-mode/v1           │
└──────────────────────────────────────────────────────────────┘

调用场景：
1. 源文档抽取 → ExtractionServiceImpl.callLlmExtract()
2. 难例判定  → TemplateFillServiceImpl.callLlmJudge()
3. AI对话    → AiServiceImpl.handleGeneralChatFlow()
4. SQL生成   → AiModelServiceImpl.getSql()

超时链路：
  前端 fetch AbortController (300s)
  → Nginx proxy_read_timeout (300s)
  → Gateway httpclient.response-timeout (300s)
  → SSE Emitter 超时 (300s)
  → RestTemplate / DashScope SDK (read-timeout=300s)
```

---

## 八、部署架构图

```
┌───────────────────────────────────────────────────────────┐
│                   Docker Compose 容器编排                  │
│                                                           │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐  │
│  │  MySQL    │ │  Redis    │ │  Nacos    │ │  Nginx    │  │
│  │  8.4.2    │ │  7.0.15   │ │  2.2.2    │ │  1.24.0   │  │
│  │  :3306    │ │  :6379    │ │  :8848    │ │  :8080    │  │
│  └───────────┘ └───────────┘ └───────────┘ └─────┬─────┘  │
│                                                  │        │
└──────────────────────────────────────────────────┼────────┘
                                                   │
                    ┌──────────────────────────────┘
                    │ 反向代理 /api/v1/**
                    ▼
┌──────────────────────────────────────────────────────────────┐
│                    JVM 进程 (Java 17)                         │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐    │
│  │ Gateway      │  │ User Service │  │ File Service     │    │ 
│  │ :18080       │→ │ :9001        │  │ :9003            │    │
│  │ Spring Cloud │  │ Spring Boot  │  │ Spring Boot      │    │
│  │ Gateway      │→ │              │  │ + Apache POI     │    │
│  └──────────────┘  └──────────────┘  └──────────────────┘    │
│         │                                                    │
│         │          ┌──────────────┐                          │
│         └────────→ │ AI Service   │                          │
│                    │ :9002        │                          │
│                    │ Spring Boot  │                          │
│                    │ + Spring AI  │ ← DashScope API          │
│                    │ + POI        │                          │
│                    └──────────────┘                          │
└──────────────────────────────────────────────────────────────┘
```

---

## 九、安全架构

```
请求安全流程：
                                                           
Browser → HTTPS(推荐) → Nginx → Gateway → [JWT过滤器]      
                                              │             
                              ┌───────────────┤             
                              │ 白名单路径     │ 非白名单    
                              │ /auth          │             
                              │ /verification  │             
                              ▼               ▼             
                          直接放行      验证JWT Token        
                                              │             
                                    ┌─────────┤             
                                    │ 有效     │ 无效/过期   
                                    ▼         ▼             
                               转发请求    401 Unauthorized  
                      (注入X-User-ID头)                      

密码安全：BCrypt加密存储 (不可逆)
Token管理：Redis存储 + 20小时有效期
API限流：通过Gateway统一管控
文件校验：类型+大小+MIME检测
XSS防护：DOMPurify消毒所有v-html渲染内容（AI响应、文档预览）
```

---

## 十、多场景填表能力说明

DocAI 支持多种复杂填表场景，以满足不同业务需求：

### 10.1 多表填写（一份模板包含多个表格）

以 Word 模板为例，一份 `.docx` 模板可能包含多个表格（如基本信息表、人员信息表、经费预算表），系统会：
1. 解析所有表格中的槽位（`parseWordSlots` 遍历所有表格、行、列）
2. 为每个槽位独立召回候选字段
3. 使用贪心分配策略：跨表相同标签优先分配不同值，避免重复填入
4. 各表独立写回，互不干扰

### 10.2 多源文档填充一份模板

支持多个不同类型的源文档共同填充同一份 Excel/Word 模板：
1. 用户上传多份源文档（docx + xlsx + txt + md 混合）
2. 所有源文档的提取字段合并到统一的候选池
3. 候选召回阶段从全量字段中检索，多文档投票机制提高匹配可信度
4. `directTableCopy` 支持跨多个源 Excel 合并数据行并去重

### 10.3 需求驱动精准填充

填表时可传入 `userRequirement` 参数（如"仅填写2024年北京地区"），系统会：
1. 解析需求中的关键词（时间/地点/类型等）
2. 候选召回增加 `requirement_match_score` 维度
3. 命中需求关键词的候选大幅加权
4. 仍有空槽位时，启用强兜底回填

---

## 相关文档

- [项目启动说明书](项目启动说明书.md) — 环境配置、启动部署、企业级部署方案
- [API接口文档](API接口文档.md) — RESTful API 完整规范
- [商业项目策划书](商业项目策划书.md) — 商业模式与市场分析
- [答辩材料](答辩材料.md) — 答辩准备与常见问答
- [PPT制作纲要](PPT制作纲要.md) — 演示PPT结构与设计

---

> **文档更新日期**：2026年3月27日
